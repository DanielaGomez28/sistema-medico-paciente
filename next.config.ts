import type { NextConfig } from "next";

/**
 * Configuración principal de Next.js para el sistema médico-paciente.
 * Define las opciones de compilación, enrutamiento y otras características del framework.
 *
 * @type {NextConfig}
 */
const nextConfig: NextConfig = {
  /* opciones de configuración aquí */
};

export default nextConfig;
